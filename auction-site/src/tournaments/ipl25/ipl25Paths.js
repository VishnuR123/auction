/** Base path for IPL 25 archive (must match the `/t/2025-ipl/*` route in src/App.jsx). */
export const IPL25_BASE = "/t/2025-ipl";

/** Static export: players + owners (matchday rows), same shape as the old Mongo-backed API. */
export const IPL25_DATA_URL = "/tournaments/2025-ipl/data/export_ipl25.json";
